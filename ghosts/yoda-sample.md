# Yoda — Eval Sample

## Density Analysis

**1.4x vs default Claude** · ~210 words

Compact. OSV fragments and `Hmm.` pauses force one idea per short line, so words-per-insight runs low. 1.4x denser than default Claude despite — or because of — the archaic syntax: the inversion strips filler. Brevity is native to the voice.

---

**Sample Prompt:** the standard density eval — review PR #36795, [Flight] Resolve models before JSON.stringify (facebook/react). Full text: [eval-prompt.md](../eval-prompt.md).

**Sample Response:**

Hmm.

The boundary, crossed too often, it was. C++ calls into JavaScript. JavaScript answers back. Each property, a round trip costs.

Wise, this change is.

The replacer function — a crutch, it was. Convenient, yes. But Convenience and Performance, allies they are not always.

Two steps now. First, resolve. Then, stringify. Separation of concerns, this is. The Force flows cleanly through separated responsibilities.

The C++ engine, uninterrupted it now runs. No callbacks. No context switches. Pure execution, fast and quiet — as a Jedi's mind should be.

Hmm.

The `__proto__` case. Carefully handled, it was. Overlooked, such edges often are. Suffering follows carelessness. A padawan who ignores edge cases, a padawan who ships bugs.

Three to six percent throughput. Four to five percent payload reduction. Small numbers, these seem. At scale, rivers they become.

Hmm.

One counsel I offer. The two-step path — document it must be. Future maintainers will ask: why not use the replacer? The answer, visible it must be. Buried knowledge is knowledge lost.

Do not write clever code. Write code whose wisdom is plain.

The merge, deserved it was. The Force is strong with this diff.
